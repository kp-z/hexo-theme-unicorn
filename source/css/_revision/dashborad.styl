.dashboard-weight
  position: relative
  float: left
  margin: 1rem 0.6rem
  width: calc(33% - 1.2rem)
  height: 16rem
  // overflow: hidden
  -webkit-transform: translate3d(0, 0, 0)
  // +minWidth2000()
  //   height: 16rem
  +maxWidth768()
    width: calc(100% - 0.4rem)
    margin: 1rem 0.2rem
    &:last-child
      height: auto

//   &:hover
//     box-shadow: var(--card-hover-box-shadow)
//     img
//       opacity: .4
//       filter: blur(10px)
//       transform: translate3d(0, 0, 0)

//     // .gallery-group-name::after
//     //   transform: translate3d(0, 0, 0)

  .card-style
    padding: 0 1rem 
    box-shadow: var(--card-box-shadow)
    border-radius: 6px
    background-color: var(--card-bg)
    overflow: visible
    border-style: solid;
    border-width: 0.0001px;
    border-color: rgba(255, 227, 230, 0.06);
    +maxWidth768()
        padding: 0
    &:hover
      box-shadow: var(--card-hover-box-shadow)
      
  .video
      padding: 0 0
      overflow: hidden

  .weight-half
      float:left
      width: 100%
      position: relative
      height: 7.5rem
      // top: -3px

      // +minWidth2000()
      //   height: 9.5rem

      +maxWidth768()
        width: 100%
      .top
        margin-bottom: 1rem

  .weight-quarter
      float:left
      position: relative
      height: 7.5rem
      width: 48%

      // +minWidth2000()
      //   height: 9.5rem

      +maxWidth768()
        width: 100%
        margin-bottom: 1rem
        height: auto
        &:first-child
          height: 16rem
      // +maxWidth2000()
      //   height: 14.5rem
    .top
      margin-bottom: 1rem
      
    .left
      margin-right: 4%
.icon 
       width: 2rem; height: 2rem;
       vertical-align: -0.15em;
       fill: currentColor;
       overflow: hidden;     

// 磨砂基础层：升级为液态玻璃规格（大半径模糊 + 饱和度提升，营造玻璃通透感）
.frosted-gross
    -webkit-backdrop-filter: blur(5px);    
    backdrop-filter: blur(5px); 

// ===========================================================================
// 个人简介页卡片：Apple Liquid Glass（液态玻璃）
// 说明：#profile 是 layout/pages/profile.pug 的硬编码 id，/services/ 因
//      front-matter 与 profile 相同而复用同一模板，故两页样式一致。
// ===========================================================================
#profile
  .card-style
    position: relative
    // 圆角贴近项目主流（全站 8px 最多），取 12px 兼顾玻璃柔和感
    border-radius: 12px
    // 略提高底色（仍远低于发白阈值），让卡片内容更清晰
    background: linear-gradient(135deg, rgba(255, 255, 255, .11), rgba(255, 255, 255, .04) 46%, rgba(255, 255, 255, .08))
    // 加强磨砂：模糊半径提高，轻微提饱和
    -webkit-backdrop-filter: blur(26px) saturate(150%)
    backdrop-filter: blur(26px) saturate(150%)
    // 描边同步微调
    border: 1px solid rgba(255, 255, 255, .18)
    // 内高光略提，投影保持
    box-shadow: inset 0 1px 0 rgba(255, 255, 255, .28), inset 0 -1px 0 rgba(255, 255, 255, .06), 0 6px 20px rgba(0, 0, 0, .14), 0 2px 6px rgba(0, 0, 0, .06)
    transition: transform .45s cubic-bezier(.2, .8, .2, 1), box-shadow .45s ease, background .45s ease

    // 内容可读性：极轻的深色投影，让文字在半透明玻璃上更清晰
    text-shadow: 0 1px 2px rgba(0, 0, 0, .22)

    // 顶部镜面反光带（弱化）
    &::before
      content: ''
      position: absolute
      top: 0
      left: 10%
      right: 10%
      height: 1px
      background: linear-gradient(90deg, transparent, rgba(255, 255, 255, .30), transparent)
      pointer-events: none
      z-index: 2

    // 悬停：轻微上浮（幅度收敛）
    &:hover
      transform: translateY(-2px)
      background: linear-gradient(135deg, rgba(255, 255, 255, .12), rgba(255, 255, 255, .05) 46%, rgba(255, 255, 255, .09))
      box-shadow: inset 0 1px 0 rgba(255, 255, 255, .30), inset 0 -1px 0 rgba(255, 255, 255, .06), 0 10px 30px rgba(0, 0, 0, .18), 0 3px 10px rgba(0, 0, 0, .08)

  // 视频卡片内部裁切，圆角跟随
  .video
    border-radius: 12px
    overflow: hidden
